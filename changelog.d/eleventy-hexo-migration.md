### Added

- `zigapagos migrate` now recognizes Eleventy (`11ty`) and Hexo projects, inventories their conventional content/template trees, and supports non-clobbering Markdown conversion with loss-visible frontmatter review metadata. Ambiguous projects stop with an actionable `--from` prompt instead of guessing from a shared config filename.
