### Changed

- Update Node.js development types to 26.6.2 across the runtime and linked
  consumer lockfiles. Audit all npm packages; Happy DOM is already current,
  and TypeScript 6 remains required by the runtime compiler API.
