# Test scripts CI knowingly does not run, one per line: <path><space(s)><reason>.
#
# tests/meta/script-coverage.sh fails on a test script that is neither run by CI nor
# listed here, and on a row here for a script that IS run or no longer exists. The point
# is not to bless the gap — it is to make adding to it a deliberate, reviewed act. Two
# assertions in this repo were found rotted in one session purely because nothing ran
# the file; see that script's header.
#
# EVERY ROW IS A DEBT, NOT A DECISION THAT AGES WELL. A row should say what it would
# cost to run the script in CI, so the trade-off can be re-checked rather than
# re-litigated from scratch.
#
# ---------------------------------------------------------------------------------
# THE INVENTORY IS EMPTY. WAS 14 ROWS, THEN 3, NOW 0 — every test script in this
# repository is run by CI. The file stays because the GATE is the point, not the list:
# script-coverage.sh reads it on every run, and an empty inventory is the state it
# exists to defend, not a reason to delete it. Where the fourteen went:
#
#   11  the non-browser examples/tsx-site/test scripts -> ci.yml's e2e-dev-loop job,
#       on a consumer build that job already pays for (~49s for all eleven).
#    1  site/test/build.sh -> pages.yml AND ci.yml's site build job, checking
#       output each already builds. The five greps measured 1.7s warm; paying a
#       separate cold consumer build (~120s historically) is unnecessary now.
#    2  examples/tsx-site/test/{hydrate,spa_slice}.sh -> browser-e2e.yml, a nightly
#       `schedule:` + `workflow_dispatch:` workflow that installs Google Chrome and runs
#       each script on its own matrix runner (own runner because spa_slice.sh opens by
#       `rm -rf`ing .zig-cache/zig-out, so the two cannot share a build). Scheduled
#       rather than PR-gating because ~125s each on top of a ~265s cold consumer build
#       is not a per-PR cost, and because what they catch — a real-browser hydration or
#       runtime-slicing regression — arrives with a runtime/src change or a preact bump,
#       unattended.
#
# PREVIOUS RESIDUAL GAPS, NOW CLOSED:
#
#  1. site/test/build.sh now gates PRs in ci.yml as well as deployment in pages.yml.
#     Its assertions reuse the PR job's site output, rather than another build.
#
#  2. All eight formerly uncalled *_playwright.py helpers (spa, spa_csp, spa_guards,
#     spa_nested, spa_nested_guarded, spa_reload, spa_scroll, spa_split) now run via
#     spa_browser.sh all in browser-e2e.yml. These read-only helpers share one build
#     and browser installation, avoiding seven redundant setups; hydrate and the
#     destructive spa_slice suite retain isolated runners. These remain nightly or
#     manual checks, not PR gates. Dispatch on a branch when changing the runtime.
#
# ADDING A ROW IS A REGRESSION. If a new script cannot be run in CI, say here what it
# would cost and what would have to change for that to stop being true.
# ---------------------------------------------------------------------------------
